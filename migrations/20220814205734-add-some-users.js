'use strict';

const {User} = require('../models/index')

module.exports = {
    async up(queryInterface, Sequelize) {
        // await User.create({ id: "dim@mail.com", password: "dima"})
        // await User.create({ id: "0503142355", password: "dima1"})
    },
    async down(queryInterface, Sequelize) {

    }
};